# bank_term_deposit_prediction
Model that's predict whether a client will subscribe to a bank term deposit.

Instruction

Unzip the dataset and put test,train csv inside the data folder, 
Open the main folder with vscode, 
Save all files, 
Run 1-4 notebook, 
Save all files, 
Select Terminal, 
Run code = "streamlit run app/streamlit_app.py"      use this to run the streamlit dashboard
